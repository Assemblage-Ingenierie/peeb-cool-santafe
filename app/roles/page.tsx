import { PagePlaceholder } from "@/components/page-placeholder";

export default function RolesPage() {
  return (
    <PagePlaceholder
      title="Gestión de roles"
      description="Asignación de roles (admin / usuario)."
    />
  );
}
